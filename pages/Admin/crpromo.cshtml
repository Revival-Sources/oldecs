<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel | Create Promocode</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #222;
            color: #fff;
            margin: 0;
            padding: 20px;
        }

        .header {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
            color: #fff;
        }

        .image-square {
            width: 200px;
            height: 200px;
            background-color: #555;
            background-image: url('/api/catalog/1/image');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            margin-bottom: 20px;
        }

        .text-input {
            width: 100%;
            padding: 10px;
            border: none;
            border-radius: 4px;
            background-color: #333;
            color: #fff;
            margin-bottom: 10px;
            font-size: 16px;
        }

        .checkbox-label {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
            color: #fff;
        }

        .checkbox-label input[type="checkbox"] {
            margin-right: 5px;
        }

        .create-button {
            background-color: #009688;
            color: #fff;
            font-size: 16px;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }

        .create-button:hover {
            background-color: #013e38;
        }
    </style>
</head>
<body>
    <div class="header">Create Promocode</div>
    <input type="text" id="id" class="text-input" placeholder="OLDECS Item ID">
        <input type="text" id="promocode" class="text-input" placeholder="Promocode">
    <input type="text" id="robux" class="text-input" placeholder="Robux">
    <input type="text" id="tix" class="text-input" placeholder="Tix">
    <input type="text" id="type" class="text-input" placeholder="Type (item or currency)">
    <button class="create-button">Create</button>
</body>
</html>